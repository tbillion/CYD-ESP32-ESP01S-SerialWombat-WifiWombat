/*
 * Global State Variables - Header
 *
 * Shared state variables used across multiple modules.
 * Extracted from original .ino file.
 */

#pragma once

// SD card support runtime flag
extern bool isSDEnabled;
